defaults:
  - data: music_genres
  - classifier: coin_net
  - architecture: tabnet_music_genres
  - ssl_model: none
  - _self_
experiment_name: "test"
# TODO: Replace path for saving results via `mlflow`.
mlruns_path: ".test"
accelerator: "cpu"
seed: 0
# TODO: Replace path for saving results via `hydra`.
hydra:
  run:
    dir: ".outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}"